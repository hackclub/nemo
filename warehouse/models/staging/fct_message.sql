select
    channel_id,
    ts,
    author_id,
    author_kind,
    subtype,
    thread_root_ts,
    is_reply,
    posted_at,
    edited_at,
    reply_count,
    reply_users_count,
    latest_reply_ts,
    reaction_count,
    reactor_count,
    file_count,
    text_length,
    mention_count,
    mentioned_ids,
    is_question,
    is_substantive,
    has_link,
    emoji_only,
    observed_at
from {{ source('raw', 'message') }}
