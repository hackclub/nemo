version: 2

models:
  - name: mart_growth
    columns:
      - name: month
        data_tests: [not_null, unique]
  - name: mart_participation
    columns:
      - name: window_end
        data_tests: [not_null, unique]
  - name: mart_channel_activity
    columns:
      - name: channel_id
        data_tests: [not_null]
