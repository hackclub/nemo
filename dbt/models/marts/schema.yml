version: 2

models:
  - name: mart_growth
    columns:
      - name: month
        data_tests: [not_null, unique]
  - name: mart_participation
    columns:
      - name: window_end
        data_tests: [not_null, unique]
  - name: mart_channel_activity
    columns:
      - name: channel_id
        data_tests: [not_null]
  - name: mart_activation
    columns:
      - name: claim_rate
        data_tests: [not_null]
  - name: mart_activity_distribution
    columns:
      - name: band_order
        data_tests: [not_null, unique]
  - name: mart_account_type
    columns:
      - name: account_type
        data_tests: [not_null]
  - name: mart_fast_reply_vs_retention
    columns:
      - name: fast_reply
        data_tests: [not_null, unique]
  - name: mart_onboarding_funnel
    columns:
      - name: cohort_month
        data_tests: [not_null, unique]
  - name: mart_channel_onboarding_scorecard
    columns:
      - name: channel_id
        data_tests: [not_null]
  - name: mart_onboarding_recurrence_funnel
    columns:
      - name: total_members
        data_tests: [not_null]
  - name: mart_message_depth_distribution
    columns:
      - name: threshold
        data_tests: [not_null, unique]
  - name: mart_channel_engagement
    columns:
      - name: channel_id
        data_tests: [not_null, unique]
