        <div class="navsec">
          <nav class="rail-nav" aria-label="Admin">
            <%= link_to admin_root_path, class: "rail-item",
                  aria: { current: ("page" if controller_path == "admin/overview") } do %>
              <span class="rail-ic" aria-hidden="true">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></svg>
              </span>
              <span class="rail-text">Overview</span>
            <% end %>
            <%= link_to admin_people_path, class: "rail-item",
                  aria: { current: ("page" if controller_path == "admin/people") } do %>
              <span class="rail-ic" aria-hidden="true">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/></svg>
              </span>
              <span class="rail-text">People</span>
            <% end %>
            <%= link_to admin_roles_path, class: "rail-item",
                  aria: { current: ("page" if %w[admin/roles admin/role_channels].include?(controller_path)) } do %>
              <span class="rail-ic" aria-hidden="true">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2 4 6v6c0 5 8 10 8 10s8-5 8-10V6z"/></svg>
              </span>
              <span class="rail-text">Roles</span>
            <% end %>
            <div class="rail-nest">
              <% Admin::RoleChannelsController::SHARED_ROLES.each do |role| %>
                <%= link_to admin_role_channels_path(role: role), class: "rail-item",
                      aria: { current: ("page" if controller_path == "admin/role_channels" &&
                        params[:role].to_s == role) } do %>
                  <span class="rail-text"><%= Authz.role_label(role) %> channels</span>
                <% end %>
              <% end %>
            </div>
            <%= link_to admin_channels_path, class: "rail-item",
                  aria: { current: ("page" if controller_path == "admin/channels") } do %>
              <span class="rail-ic" aria-hidden="true">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 9h16M4 15h16M10 3 8 21M16 3l-2 18"/></svg>
              </span>
              <span class="rail-text">Channels</span>
            <% end %>
            <%= link_to admin_flags_path, class: "rail-item",
                  aria: { current: ("page" if controller_path == "admin/flags") } do %>
              <span class="rail-ic" aria-hidden="true">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 3v18M6 4h11l-2 4 2 4H6"/></svg>
              </span>
              <span class="rail-text">Flags</span>
            <% end %>
          </nav>
        </div>
