<% channels.each do |channel| %>
  <tr>
    <td>
      <span class="two-line">
        <%= link_to channel_path(channel) do %>
          <b>#<%= channel.name %></b>
        <% end %>
        <span><%= channel.channel_id %></span>
      </span>
    </td>
    <td>
      <% if channel.range_members %>
        <%= number_with_delimiter(channel.range_members) %>
      <% else %>
        <span class="chip chip-off">n/a</span>
      <% end %>
    </td>
    <td>
      <% if channel.range_messages %>
        <%= number_with_delimiter(channel.range_messages) %>
      <% else %>
        <span class="chip chip-off">no range</span>
      <% end %>
    </td>
    <td><%= channel.range_posters ? number_with_delimiter(channel.range_posters) : "n/a" %></td>
    <td><%= channel.date_created&.strftime("%b %-d, %Y") %></td>
  </tr>
<% end %>
