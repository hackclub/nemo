<h1>Access denied</h1>

<% if @message == "no_access" %>
  <p>You do not have access to Mnemosyne. Ask a community manager for it.</p>
<% else %>
  <p>Something went wrong signing in.</p>
  <p><span class="chip chip-off"><%= @message %></span></p>
<% end %>

<% if current_staff %>
  <%= button_to "Sign out", logout_path, method: :delete, class: "btn w-full" %>
<% else %>
  <%= link_to "Back to sign in", login_path, class: "btn w-full" %>
<% end %>
