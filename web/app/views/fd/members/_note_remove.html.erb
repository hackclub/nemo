<% if note.author == current_account.user_id %>
  <%= button_to "Remove", fd_member_note_path(user_id, note), method: :delete,
        class: "btn btn-sm", form: { class: "contents",
        data: { turbo_confirm: "Remove this note?" } } %>
<% end %>
