<%= button_to "Remove", fd_case_participant_path(kase, person.user_id),
      method: :delete, params: { role: person.role },
      class: "text-btn", aria: { label: "take @#{person.user_id} off the case" },
      form: { class: "contents", data: { turbo_frame: "_top",
      turbo_confirm: "Take @#{person.user_id} off this case?" } } %>
