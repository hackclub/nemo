<% content_for :page_title, "Merge #{pluralize(@plan.all.size, 'case')}" %>
<% content_for :page_face do %>
  <span class="crumb"><%= link_to "Cases", fd_cases_path(@carry) %> /</span>
<% end %>

<div class="card card-flush">
  <%= render "ticked", plan: @plan, carry: @carry %>
</div>
