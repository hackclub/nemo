<% @candidates.each do |other| %>
  <%= render "fd/merges/candidate", other: other, violations: @violations,
        priors_by_user: @priors, reachable: @reachable %>
<% end %>
<% if @more %>
  <template data-more-next="<%= fd_case_merge_path(@case, page: @page + 1) %>"></template>
<% end %>
