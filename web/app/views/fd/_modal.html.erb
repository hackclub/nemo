<div class="modal-host">
  <input type="checkbox" id="<%= id %>" class="modal-flip" autocomplete="off"
         <%= "checked" if local_assigns[:open] %>>
  <label for="<%= id %>" class="modal-veil" aria-hidden="true"></label>
  <div class="modal-wrap">
    <div class="modal" role="dialog" aria-label="<%= local_assigns[:label] || title %>">
      <div class="modal-head">
        <h2 class="modal-title"><%= title %></h2>
        <% if local_assigns[:sub].present? %>
          <span class="modal-sub"><%= sub %></span>
        <% end %>
      </div>
      <%= yield %>
    </div>
  </div>
</div>
