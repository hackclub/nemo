<% first_poster = top_posters.first %>
<% full_days = first_poster ? Date.new(first_poster.month.year, first_poster.month.month, -1).day : nil %>

<div class="card card-flush">
  <div class="card-head">
    <div>
      <h2 class="card-title">Top posters<%= ", #{first_poster.month.strftime("%B %Y")}" if first_poster %></h2>
      <% if first_poster %>
        <p class="card-sub">
          <%= first_poster.days_in_window %> of <%= full_days %> days
          <%= "(month in progress)" if first_poster.days_in_window < full_days %>
        </p>
      <% end %>
    </div>
    <% if top_poster_months.present? %>
      <div class="card-tools">
        <select
          class="select"
          aria-label="month"
          data-top-posters-target="select"
          data-action="top-posters#change"
        >
          <% top_poster_months.each do |m| %>
            <option value="<%= m.iso8601 %>" <%= "selected" if top_posters_month == m %>><%= m.strftime("%b %Y") %></option>
          <% end %>
        </select>
      </div>
    <% end %>
  </div>

  <div class="scroll">
    <table class="data-table">
      <thead>
        <tr>
          <th class="col-rank">rank</th>
          <th>member</th>
          <th class="col-num">messages</th>
          <th>share of the top ten</th>
        </tr>
      </thead>
      <tbody>
        <% peak = top_posters.map(&:messages_posted).max.to_i %>
        <% total = top_posters.sum(&:messages_posted) %>
        <% if top_posters.any? %>
          <% top_posters.each do |p| %>
            <tr>
              <td class="col-rank"><%= p.rank %></td>
              <td>
                <span class="two-line">
                  <b><%= p.display_name.presence || "n/a" %></b>
                  <span><%= p.user_id %></span>
                </span>
              </td>
              <td class="col-num"><%= number_with_delimiter(p.messages_posted) %></td>
              <td style="width: 38%">
                <div class="inbar">
                  <span><i style="width: <%= peak.positive? ? [(p.messages_posted.to_f / peak * 100), 1.5].max.round(1) : 0 %>%"></i></span>
                  <span><%= total.positive? ? number_to_percentage(p.messages_posted.to_f / total * 100, precision: 0) : "0%" %></span>
                </div>
              </td>
            </tr>
          <% end %>
        <% else %>
          <tr><td colspan="4">no top posters loaded</td></tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
