<div class="card">
  <div class="card-head">
    <div>
      <h2 class="card-title">Invited and claimed</h2>
      <p class="card-sub">an account is claimed when its member first signs in</p>
    </div>
  </div>
  <div
    data-controller="svg-chart"
    data-svg-chart-kind-value="bars"
    data-svg-chart-switchable-value="true"
    data-svg-chart-height-value="240"
    data-svg-chart-data-value="<%= {
      labels: growth_months.map { |m| m.month.strftime("%b %Y") },
      datasets: [
        { label: "invited", data: growth_months.map(&:invited_members) },
        { label: "claimed", data: growth_months.map(&:claimed_members) }
      ]
    }.to_json %>"
  ></div>
  <div
    data-controller="svg-chart"
    data-svg-chart-kind-value="line"
    data-svg-chart-height-value="120"
    data-svg-chart-pct-value="true"
    data-svg-chart-data-value="<%= {
      labels: growth_months.map { |m| m.month.strftime("%b %Y") },
      datasets: [{ label: "claim rate", data: growth_months.map { |m| m.claim_rate && m.claim_rate.to_f * 100 } }]
    }.to_json %>"
  ></div>
</div>
