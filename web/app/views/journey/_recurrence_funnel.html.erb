<div class="card">
  <div class="card-head">
    <div>
      <h2 class="card-title">Onboarding recurrence</h2>
      <% if recurrence_funnel %>
        <p class="card-sub">of <%= number_with_delimiter(recurrence_funnel.total_members) %> total known members</p>
      <% end %>
    </div>
    <% if recurrence_cohort_months.present? %>
      <div class="card-tools">
        <% if recurrence_funnel %>
          <% coverage = recurrence_funnel.signed_in.to_i.positive? ? recurrence_funnel.knowable.to_f / recurrence_funnel.signed_in : nil %>
          <%= incomplete_chip(
                ("#{number_to_percentage(coverage * 100, precision: 0)} of this cohort known" if coverage && coverage < 1)
              ) %>
        <% end %>
      </div>
    <% end %>
  </div>

  <% if recurrence_funnel %>
    <div class="chart-short">
      <div
        data-controller="chart"
        data-chart-kind-value="bars"
        data-chart-height-value="240"
        data-chart-data-value="<%= {
          labels: ["Created", "Joined", "Posted", "2nd post", "3rd post"],
          datasets: [{
            label: "members",
            data: [
              recurrence_funnel.created_account,
              recurrence_funnel.signed_in,
              recurrence_funnel.posted_once,
              recurrence_funnel.posted_twice,
              recurrence_funnel.posted_three_times
            ]
          }]
        }.to_json %>"
      ></div>
    </div>
  <% else %>
    <p class="card-note">no data loaded yet</p>
  <% end %>
</div>
