<% content_for :page_title, "Coming back" %>

<div id="retention">
  <%= render "cohort_retention", retention: @retention %>
</div>

<%= render "recurrence_funnel", recurrence_funnel: @recurrence_funnel,
      recurrence_cohort_months: @recurrence_cohort_months, recurrence_month: @recurrence_month %>
